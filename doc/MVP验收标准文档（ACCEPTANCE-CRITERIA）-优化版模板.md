# 氛围日记 (Vibe Diary) - MVP 验收标准文档
### （Vibe Coding 完成即达标）

---

## 📋 AI 阅读指南

本文档是 **Vibe Coding** 开发的唯一验收依据，所有功能必须逐条通过才能判定开发完成。

- **验收编号体系**：`AC-xxx` 与 PRD 中的 `F-xxx` 功能编号一一对应。
- **验收结构**：每条验收项包含 **操作步骤 → 预期结果 → 判定标准**，确保无歧义。
- **完成定义**：**全部通过 = 开发完成**，任何一条不通过 = 回退修复。

**核心库路径**：`/Users/zhengyusheng/Desktop/diary/StPageFlip-master`

**关联文档**：
- PRD（需求来源）
- CONSTRAINTS（约束依据）
- TODO-LIST（问题追踪）

---

## 变更日志

| 版本 | 日期 | 变更内容 | 变更人 |
| :--- | :--- | :--- | :--- |
| v1.0 | 2026-04-10 | 初始版本，确立拟物化书架与物理翻页验收项 | Gemini |

---

## 一、功能验收标准

> ⚠️ **全部通过才算完成**。编号 `AC-xxx` 对应 PRD 中的 `F-xxx` 功能。

### AC-001：拟物化虚拟书架 (F-001) 【必过】

| 场景 | 操作步骤 | 预期结果 | 判定标准 |
| :--- | :--- | :--- | :--- |
| **正常流程** | 启动项目进入首页 (P-001) | 页面显示渐变米色背景，书本整齐排列在白色隔板上 | ✅ 背景色与 Stitch 设计稿一致 |
| **视觉一致性** | 对比 `/stitch_vibe_diary_bookshelf` 预览图 | 书本阴影、材质感、字体与设计稿高度还原 | ✅ 视觉误差 < 5% |
| **响应式适配** | 在浏览器中从 PC 宽度缩放至手机宽度 | 书架列数自适应调整（如 4 列变 1 列），布局无遮挡 | ✅ 栅格系统响应正常 |
| **交互跳转** | 点击任意一本存在的日记封面 | 页面平滑跳转至阅读页 (P-002) | ✅ 路由跳转及 ID 传参正常 |

### AC-002：StPageFlip 沉浸式翻页引擎 (F-002) 【必过】

| 场景 | 操作步骤 | 预期结果 | 判定标准 |
| :--- | :--- | :--- | :--- |
| **源码调用校验** | 检查 `package.json` 及控制台加载日志 | 确认插件加载自 `/Users/zhengyusheng/Desktop/diary/StPageFlip-master` | ✅ 引用本地源码而非 npm |
| **物理模拟交互** | 鼠标拖拽或手指滑动日记本页面边缘 | 页面产生动态弧度卷起，背面显示动态阴影，且有物理张力感 | ✅ 物理反馈真实，无卡顿 |
| **模式适配** | 在 PC 端（宽屏）与手机端（窄屏）分别打开 | PC 端显示双页模式，手机端自动切换为单页模式 | ✅ 符合 StPageFlip 适配逻辑 |

### AC-003：日记内容编辑与持久化 (F-003) 【必过】

| 场景 | 操作步骤 | 预期结果 | 判定标准 |
| :--- | :--- | :--- | :--- |
| **正常流程** | 在日记页内输入一段文字并翻页 | 输入顺畅，文字正确渲染在 Canvas/DOM 上 | ✅ 内容展示无误 |
| **自动保存** | 输入文字后停止操作 2 秒，然后刷新页面 | 重新进入该页，文字内容完整保留 | ✅ SQLite (`vibe.db`) 写入成功 |
| **数据库完整性** | 使用工具打开 `vibe.db` 文件的 `T-002` 表 | 看到对应日记的 `content` 字段已实时更新 | ✅ 字段结构符合 T-002 定义 |

### AC-004：封面材质与样式自定义 (F-004) 【可选】

| 场景 | 操作步骤 | 预期结果 | 判定标准 |
| :--- | :--- | :--- | :--- |
| **材质切换** | 在设置面板中选择“皮革”或“亚麻”材质 | 首页对应书本的封面纹理实时发生变化 | ✅ 样式 ID 正确更新至 T-001 |

---

## 二、性能验收标准

> ⚠️ **量化指标**，确保 Vibe Coding 的高质量交付。

| 编号 | 指标要求 | 验证方法 | 判定标准 |
| :--- | :--- | :--- | :--- |
| **PERF-001** | 翻页动画流畅度 ≥ 60 FPS | Chrome DevTools Rendering 面板监控 | ✅ 稳定无掉帧 |
| **PERF-002** | 首屏加载时间 ≤ 1.5 秒 | Network 面板查看 `DOMContentLoaded` 时间 | ✅ ≤ 1.5s |
| **PERF-003** | 接口响应时间 ≤ 2.0 秒 | 检查保存 (Save) 接口的返回耗时 | ✅ 所有核心接口达标 |
| **PERF-004** | 内存占用 | 连续翻页 50 次无明显内存增量 | ✅ 无内存泄漏 (Chrome Memory 面板) |

---

## 三、兼容性 & 异常验收标准

| 编号 | 类别 | 测试场景 | 预期结果 | 判定标准 |
| :--- | :--- | :--- | :--- | :--- |
| **COMP-001** | 设备兼容 | iOS (Safari) 与 Android (Chrome) | 翻页 Canvas 渲染正常，无显示异常 | ✅ 双端均正常 |
| **COMP-002** | 响应式 | iPhone 刘海屏 / iPad 分屏 | 底部 Safe Area 不遮挡书架底部的操作区域 | ✅ 适配安全区域 |
| **ERR-001** | 库丢失异常 | 模拟本地 `StPageFlip` 源码路径失效 | 页面显示提示“引擎初始化失败”，不崩溃 | ✅ 优雅降级 |
| **ERR-002** | 安全校验 | 在日记内容中输入 `<script>` 标签 | 内容被安全转义，不执行非法脚本 | ✅ XSS 过滤生效 |

---

## 四、代码规范验收标准

| 编号 | 检查项 | 要求 | 判定标准 |
| :--- | :--- | :--- | :--- |
| **CODE-001** | 依赖硬编码 | `package.json` 必须指向本地 `/StPageFlip-master` 文件夹 | ✅ 非远程依赖 |
| **CODE-002** | 文件行数 | 单个 Vue 组件或 Express API 文件 ≤ 500 行 | ✅ 逻辑结构清晰 |
| **CODE-003** | SQL 安全 | 所有数据库查询必须使用 `?` 占位符进行参数化 | ✅ 无拼接注入风险 |
| **CODE-004** | 资源管理 | 所有图片资源（纹理等）大小 ≤ 200KB | ✅ 加载性能达标 |

---

## 五、Vibe Coding 完成判定标准

- [ ] **AC 全通过**：第一章所有核心功能（`AC-001` 至 `AC-003`）全部通过。
- [ ] **视觉全一致**：对比 Stitch 界面，色彩、阴影还原度 > 95%。
- [ ] **PERF 全达标**：翻页动画在 iPhone 模拟器上保持 60 帧丝滑感。
- [ ] **无越界开发**：未添加任何 PRD 范围外的多余社交或同步功能。

---

## 六、验收不通过处理流程

```text
验收不通过
    ↓
记录到待办清单 (VC-xxx)，标注优先级 P0
    ↓
定位问题原因 (关联 PRD F-xxx / CONSTRAINTS BAN-xxx)
    ↓
修复问题并重新同步 StPageFlip-master 引用
    ↓
重新执行对应验收项 (AC-xxx)
    ↓
通过 → 归档到已解决